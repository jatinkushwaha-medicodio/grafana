apiVersion: 1

# Default route: everything → teams-all. Grouping keeps a burst of the same
# alert to one message per 5 min.

policies:
  - orgId: 1
    receiver: teams-all
    group_by: [alertname, env]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
