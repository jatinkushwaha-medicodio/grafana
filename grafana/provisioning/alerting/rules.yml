apiVersion: 1

# Alert rules — rebuilt in Phase 1d against Medicodio metric names.
# Placeholder targets (once /metrics + job instrumentation ships):
#   - Job staleness (app_job_last_success_timestamp_seconds too old)
#   - 5xx spike (app_http_request_duration_seconds_count status_class=5xx)
#   - p95 latency high (histogram_quantile over app_http_request_duration_seconds_bucket)
#   - Monitoring VM disk > 80% (node_filesystem_avail_bytes)

groups: []
